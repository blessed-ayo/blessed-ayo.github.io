import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/',  // added for GitHub Pages. Set to '/' because repo is exactly blessed-ayo.github.io
})
